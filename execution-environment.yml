---
version: 1

build_arg_defaults:
  # Menggunakan base image minimal RHEL 9
  EE_BASE_IMAGE: 'registry.redhat.io/ansible-automation-platform-24/ee-minimal-rhel9:latest'

ansible_config: 'ansible.cfg'

dependencies:
  galaxy: requirements.yml
